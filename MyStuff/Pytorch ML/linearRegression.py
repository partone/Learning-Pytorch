import torch
import numpy as np
import matplotlib.pyplot as plt     # Plotting library

import torch.nn as nn       # For faster syntax

X = torch.linspace(1, 50, 50).reshape(-1, 1)           # 50 linearly spaced elements
